package model.rooms;

import model.Room;
import model.RoomType;

public class King extends Room {
    public King(String roomNumber, Double price, RoomType enumeration) {
        super(roomNumber, price, enumeration);
    }
}
